# Local Maven cache — recreated by Maven after clone
**/.m2/

# Build outputs
**/target/
**/node_modules/

# Nested git metadata renamed while assembling this repo
**/.git.bak/

# Partial extract inside the split archive (the .z* / .zip volumes are the archive)
split_SWS/SWS/

# IDE / OS
.idea/
**/.idea/
*.iml
.classpath
.project
.settings/
*.log
.DS_Store
Thumbs.db
*.crdownload
